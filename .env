# iDRAC host and SNMP credentials
IDRAC_HOST=x.x.x.x
IDRAC_USER=root
IDRAC_PASS=calvin   # only used if switching to SNMPv3 later
SNMP_COMMUNITY=public

# Control mode: continuous or oneshot
MODE=continuous

# Interval in seconds (only for continuous mode)
INTERVAL=30

# Max safe temperature before reverting to auto-control
SAFE_TEMP=75
